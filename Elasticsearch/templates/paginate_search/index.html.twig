{% extends 'base.html.twig' %}

{% block title %}Hello PaginateSearchController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {% if pagination.start > 1 %}
      <a href="{{ path("paginate_search") }}/{{ pagination.startTotal  }}">{{ pagination.startTotal }}</a>...
    {% endif %}

    <a href="{{ path("paginate_search") }}/{{ pagination.start  }}">{{ pagination.start }}</a>
    <a href="{{ path("paginate_search") }}/{{ pagination.middle }}">{{ pagination.middle }}</a>
    <a href="{{ path("paginate_search") }}/{{ pagination.end }}">{{ pagination.end }}</a>

    {% if pagination.end != pagination.endTotal  %}
        ... <a href="{{ path("paginate_search") }}/{{ pagination.endTotal }}">{{ pagination.endTotal }}</a>
    {% endif %}


    <b>{{ id }}</b>
</div>
{% endblock %}
